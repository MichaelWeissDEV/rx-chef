# Remove EXIF

Removes EXIF data from a JPEG image.

EXIF data embedded in photos usually contains information about the image file itself as well as the device used to create it.

- Input: `Bytes`
- Output: `Bytes`
- CLI: `rxchef run "Remove EXIF"`
- Arguments: none

