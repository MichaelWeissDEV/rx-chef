# Parse UDP

Parses a UDP header and payload (if present).

- Input: `String`
- Output: `JSON`
- CLI: `rxchef run "Parse UDP"`

## Arguments

| # | Argument | Default | Description |
|---:|---|---|---|
| 1 | Input format | `Hex` | Format of the input data |

