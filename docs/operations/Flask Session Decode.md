# Flask Session Decode

Decodes the payload of a Flask session cookie (itsdangerous) into JSON.

- Input: `String`
- Output: `JSON`
- CLI: `rxchef run "Flask Session Decode"`

## Arguments

| # | Argument | Default | Description |
|---:|---|---|---|
| 1 | View Timestamp | `false` | Include the timestamp in the output |

